# This is a boilerplate .npmrc file including common settings with explanations.
# Note that not all possible options are included, and you should customize this
# according to your project's needs.

# Set the registry URL to use for all npm package requests
# Default is the public npm registry.
registry=https://registry.npmjs.org/

# Set a custom CA certificate, useful for self-signed certificates in private registries
# cafile=/path/to/your/ca.pem

# Configure the email address to associate with your npm account
# This is used when you npm publish
email=lee@alienfishconsulting.com

# Set the access level of published packages
# Can be "public" or "restricted"
# Default is "restricted" for scoped packages and "public" for unscoped packages.
access=public

# Save exact versions of dependencies instead of using npm's default semver range operator
save-exact=true

# Set the default save prefix for dependencies in package.json
# The default is "^" which saves the package with a caret range
save-prefix='~'

# Specify the package-lock.json usage
# It's strongly recommended to set this to true to ensure reproducible installs
package-lock=true

# Specify whether to generate an npm-shrinkwrap.json file
# Similar to package-lock.json but publishable
shrinkwrap=true

# Configure how npm handles symbolic links to local packages
# When set to true, symbolic links are treated as linked packages
bin-links=true

# Set the number of retries for network requests
fetch-retries=2

# Set the base backoff delay (in ms) for network requests
fetch-retry-mintimeout=10000

# Set the maximum backoff delay (in ms) for network requests
fetch-retry-maxtimeout=60000

# Configure the log level
# Valid values are: silent, error, warn, http, info, verbose, silly
loglevel=warn

# Set the maximum number of parallel network requests
# Default is 50
maxsockets=50

# Enable/disable progress bar display
progress=true

# Set the proxy server for HTTP requests
# proxy=http://<username>:<password>@<proxy-server-url>:<port>

# Set the proxy server for HTTPS requests
# https-proxy=http://<username>:<password>@<proxy-server-url>:<port>

# Specify if SSL certificates are strictly enforced
# Default is true
strict-ssl=true

# Set the timeout (in milliseconds) for npm search
searchlimit=20

# Set the time (in milliseconds) to wait for a response from the registry
timeout=30000

# Set the user agent string sent to the npm registry
# Default is "npm/{npm-version} node/{node-version} {platform} {arch}"
user-agent=npm/{npm-version} node/{node-version} {platform} {arch}

# Set the npm user for running commands with --global
# Useful for global installs in system-wide directories
# user=root

# Specify a folder for global installations
# Default locations are system-specific
# prefix=/path/to/global/install/location